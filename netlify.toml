# Netlify config — drop this repo (or the dist folder) and it just works.
[build]
  command = "npm run build"
  publish = "dist"

# SPA-safe fallback (harmless; the app has no client routing today).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
